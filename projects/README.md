This directory can be used to store files for pipeline projects.

The `test` folder contains files used for testing features of the pipeline.

The `example` folder contains files with example pipeline projects.